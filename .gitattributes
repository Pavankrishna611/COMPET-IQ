# Auto detect text files and perform LF normalization
* text=auto

# Force LF line endings for shell scripts (critical for Linux deployment)
*.sh text eol=lf
Procfile text eol=lf
Dockerfile text eol=lf
*.toml text eol=lf
