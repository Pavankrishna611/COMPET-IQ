services:
  - type: web
    name: competiq-fullstack
    env: python
    buildCommand: npm install && cd backend && pip install -r requirements.txt && cd .. && npm run build
    startCommand: bash ./start.sh
    envVars:
      - key: PORT
        value: 10000
